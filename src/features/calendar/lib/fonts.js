// Calendar-module font stacks (experiment/clickup-ui-refresh).
// Loaded from Google Fonts in index.html; DM Sans/Mono kept as fallbacks so
// the module degrades to the app-wide fonts if the extra families fail to load.

export const FONT_HEADING = "'Space Grotesk', 'DM Sans', sans-serif"
export const FONT_BODY = "'Inter', 'DM Sans', sans-serif"
export const FONT_MONO = "'JetBrains Mono', 'DM Mono', monospace"
